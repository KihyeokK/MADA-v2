{
  "dependencies": {
    "@types/supertest": "^2.0.12",
    "supertest": "^6.3.1"
  },
  "scripts": {
    "start": "cd ./backend && npm install --production && npm run build && node ./build/index.js"
  }
}
